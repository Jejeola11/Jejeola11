const {execFileSync}=require('node:child_process');const fs=require('node:fs');const os=require('node:os');const path=require('node:path');const tmp=fs.mkdtempSync(path.join(os.tmpdir(),'fuse-source-'));execFileSync('git',['init',tmp],{stdio:'inherit'});execFileSync('git',['-C',tmp,'fetch','--depth','1','https://github.com/Jejeola11/Jejeola11.git','f512948d68460eb4b98f19b1c8e14ec051cc19e4'],{stdio:'inherit'});execFileSync('git',['-C',tmp,'checkout','FETCH_HEAD'],{stdio:'inherit'});for(const entry of fs.readdirSync(tmp)){if(['.git','vercel.json','node_modules'].includes(entry))continue;fs.cpSync(path.join(tmp,entry),path.join(process.cwd(),entry),{recursive:true});}fs.rmSync(tmp,{recursive:true,force:true});