# Fuse Auto — deploy this folder to its own Netlify site.
[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Checks every 30 min for "follow-ask" DMs nobody tapped, sends the reminder.
[functions."reminder"]
  schedule = "*/30 * * * *"
